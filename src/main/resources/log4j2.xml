<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>
        <!-- Writes to a text file -->
        <RollingFile name="FileLogger" fileName="${sys:app.logDir}/app.log"
                     filePattern="${env:LOCALAPPDATA}/TradeStatsMaster/logs/app-%d{yyyy-MM-dd}-%i.log.gz" append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} %-5p|| %c{1} - %m%n"/>
            <Policies>
                <!-- rollover daily -->
                <TimeBasedTriggeringPolicy/>

                <!-- or when file exceeds 5MB -->
                <SizeBasedTriggeringPolicy size="5MB"/>
            </Policies>
            <!-- delete old logs -->
            <DefaultRolloverStrategy max="7"/>
        </RollingFile>

        <!-- Console output (optional) -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss} %-5p|| %c{1} - %m%n"/>
        </Console>
    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="FileLogger"/>
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>